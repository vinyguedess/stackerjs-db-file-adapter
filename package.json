{
    "name": "stackerjs-db-file-adapter",
    "description": "Database manager build over OS folders and files",
    "version": "0.1.0",
    
    "dependencies": {},
    "devDependencies": {
        "babel-cli": "^6.26.0",
        "babel-core": "^6.26.0",
        "babel-preset-env": "^1.6.1",
        "chai": "^4.1.2",
        "mocha": "^5.0.1"
    },

    "scripts": {
        "build": "./node_modules/.bin/babel src -d lib",
        "test": "istanbul cover ./node_modules/mocha/bin/_mocha test -- --compilers js:babel-core/register"
    }
}
